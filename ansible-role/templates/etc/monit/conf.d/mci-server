check process mci-server with pidfile {{ mci_server_pidfile_path }}
  start program "/usr/sbin/service mci-server start"
  stop program "/usr/sbin/service mci-server stop"
  if 5 restarts within 5 cycles then timeout
